# Spike 報告：Python

- **狀態：** 🔄 進行中（P1）
- **期間：** —
- **執行平台：** Windows 11
- **棧定位：** 探路工具（**不建議**做正式產品），目的是最快鎖定硬體真相並建立基準線

> 測試規程見 [`../plan.md`](../plan.md) §6。硬體事實見 [`../hardware.md`](../hardware.md)。

---

## 目前已量測（P1 進行中）

### 環境

| 項目 | 版本 |
|---|---|
| Python | 3.14.6 |
| sherpa-onnx | **1.13.8**（`sherpa_onnx-1.13.8-cp314-cp314-win_amd64`） |
| onnxruntime | 1.28.0 |
| numpy | 2.5.1 |
| OpenCC | 0.1.7（`opencc-python-reimplemented`，純 Python） |

⚠️ **本機的 `pip install` / `pip download` 會失敗**（`PermissionError [Errno 13]`
在 `pip-unpack-*/<pkg>.whl.metadata`，即使把 TEMP 指到工作區內也一樣）。
但純 Python 寫檔與網路都正常 → 改用 [`tools/p1/fetch_wheels.py`](../../tools/p1/fetch_wheels.py)
自行下載 wheel（驗 sha256）並解壓到 `third_party/`，繞過 pip 的安裝流程。

### 模型

`sherpa-onnx-sense-voice-zh-en-ja-ko-yue-int8-2024-07-17`（239 MB int8）

選它的理由：zh/en/ja/ko/yue 多語言、支援 ITN（數字正規化）、
反應快，且**與商業競品用的是同一個模型家族**（`hardware.md` §8.6）。

### 已驗證的管線（`--smoke`）

模型內附 5 個測試音檔，全部正確解碼：

| 語言 | 音長 | 解碼 | RTF |
|---|---|---|---|
| en | 7.15s | 133 ms | 0.02 |
| ja | 7.20s | 126 ms | 0.02 |
| ko | 4.61s | 85 ms | 0.02 |
| yue | 5.15s | 92 ms | 0.02 |
| zh | 5.59s | 99 ms | 0.02 |

模型載入 814 ms。**RTF 0.02 → 5 秒語音約 0.1 秒解碼，遠低於 1.5 秒門檻。**

### 🔴 重大發現：SenseVoice 輸出**簡體中文**

實測 `zh.wav` → `开饭时间早上9点至下午5点。`（簡體）。

對台灣使用者而言這是**必須處理的問題**：
直接把簡體輸出跟繁體參考比對，CER 會爆高，但那是**字形差異不是辨識錯誤**。
→ 產品在 ASR 之後需要一道**簡→繁轉換**（OpenCC `s2t`），
   `asr_bench.py` 已把「原樣／統一繁／統一簡」三種基準都算出來，
   避免把字形問題誤判成辨識問題。

### 「準確率 ≥ 95%」的定義（計畫原本沒寫）

本工具同時回報三種定義，避免後續各說各話：

| 指標 | 定義 |
|---|---|
| 字元錯誤率 CER | 編輯距離 / 參考字數（**主指標**，業界標準） |
| 字元正確率 | `1 - CER` → 對應「≥ 95%」的說法 |
| 完全相符句比例 | 整句一字不差的句子比例（最嚴格） |

---

## T1–T7 結果

| 編號 | 測試 | 通過門檻 | 結果 | 證據／備註 |
|---|---|---|---|---|
| T1 | 裝置列舉 | 能看到藍牙麥克風 | ✅ | `hardware.md` §2 |
| T2 | 指定裝置錄音 | 5s 16k mono WAV，回放正確 | ✅ | **16 kHz 寬頻**，`hardware.md` §4.1 |
| T3 | 全域熱鍵 | 三平台事件正確 | 🟡 | 鍵碼已定案（錄音鍵 = Right Ctrl）；**尚未實作熱鍵層** |
| T4 | ASR 準確率 | 中文 ≥ 95% | ⏳ | 管線已通，**待錄 20 句測試集** |
| T5 | ASR 延遲 | ≤ 1.5 s | 🟡 | 解碼 RTF 0.02（遠低於門檻）；尚未含 VAD／注入 |
| T6 | 文字注入 | 記事本 100% | ⬜ | 未開始 |
| T7 | 打包安裝 | 三平台可安裝啟動 | ⬜ | 未開始 |
| 附加 | 資源 | 啟動 ≤ 2s、閒置 ≤ 300 MB | ⬜ | 模型載入 814 ms；記憶體待量 |

### 附帶發現：收音位準偏低會直接讓 ASR 失效

音量偏低的那次錄音（−48.8 dBFS）丟進 SenseVoice，結果是**空字串**「。」，
不是辨識錯誤，而是**什麼都沒聽到**。
→ 收音位準不是舒適度問題，是**功能性問題**；`record_testset.py` 因此在錄音當下
就即時回報位準並警告。


---

## 使用的套件與版本

| 用途 | 套件 | 版本 |
|---|---|---|
| 錄音 | `sounddevice` | |
| 重採樣 | `scipy` / `numpy` | |
| VAD | `silero-vad` | |
| ASR | `faster-whisper` 或 sherpa-onnx python | |
| 熱鍵 | `pynput` | |
| 注入／剪貼簿 | `pyperclip` + `pyautogui` | |
| UI | PySide6 | |

---

## T1–T7 結果

| 編號 | 測試 | 通過門檻 | 結果 | 證據／備註 |
|---|---|---|---|---|
| T1 | 裝置列舉 | 能看到藍牙麥克風 | ⬜ | |
| T2 | 指定裝置錄音 | 5s 16k mono WAV，回放正確 | ⬜ | native rate: ___ |
| T3 | 全域熱鍵 | 三平台事件正確 | ⬜ | 鍵碼: ___ |
| T4 | ASR 準確率 | 中文 ≥ 95% | ⬜ | 實測 ___% |
| T5 | ASR 延遲 | ≤ 1.5 s | ⬜ | 實測 ___ ms |
| T6 | 文字注入 | 記事本 100% | ⬜ | |
| T7 | 打包安裝 | 三平台可安裝啟動 | ⬜ | |
| 附加 | 資源 | 啟動 ≤ 2s、閒置 ≤ 300 MB | ⬜ | |

---

## 評分（1–5）

| 維度 | 權重 | 分數 | 理由 |
|---|---|---|---|
| 三平台原生能力（錄音/熱鍵/注入） | 25% | | |
| 打包與分發 | 20% | | |
| ASR 生態整合 | 20% | | |
| 性能與資源 | 15% | | |
| 長期維護性 | 10% | | |
| Vibe Coding 效率 | 10% | | |
| **加權總分** | 100% | | |

---

## 致命缺陷

> 「致命缺陷」＝ 會導致某平台無法達成「按住 → 說 → 放開 → 出字」。

（無 / 列出）

---

## 結論

- 值得繼續投入產品化？ ⬜ 是 ⬜ 否
- 理由：
