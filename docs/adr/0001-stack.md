# ADR 0001：正式技術棧選擇

- **狀態：** 🟡 Proposed（**尚未決策** — 等待 P1/P2/P3 Spike 結果）
- **日期：** 2026-02（建立）
- **決策者：** 專案擁有者
- **關聯文件：** [`../plan.md`](../plan.md) §4–§6、[`../hardware.md`](../hardware.md)

---

## 背景

需要一個跨平台（Windows / macOS / Linux）的常駐 Tray 桌面程式，具備：

1. 從**指定**藍牙音訊裝置錄音
2. 全域按鍵按下／放開偵測（**任意鍵碼**，見 AGENTS.md §8）
3. 本地 ASR 推論（sherpa-onnx）
4. 對前景視窗注入文字（剪貼簿貼上 + 逐字輸入退路）
5. 單一、可安裝、不被防毒誤報的安裝包

三個候選：**Rust + Tauri 2**、**Python**、**Go**。

---

## 決策

> ⏳ **尚未決定。** 本節在 P4 填寫，且必須附上 §評分表 的實測分數。

---

## 評分表（Spike 後填，1–5 分）

| 維度 | 權重 | Python | Rust + Tauri | Go |
|---|---|---|---|---|
| 三平台原生能力（錄音 / 熱鍵 / 注入） | 25% | | | |
| 打包與分發 | 20% | | | |
| ASR 生態整合 | 20% | | | |
| 性能與資源（記憶體 / 啟動 / 延遲） | 15% | | | |
| 長期維護性 | 10% | | | |
| Vibe Coding 效率 | 10% | | | |
| **加權總分** | 100% | | | |

**已預先記錄的已知事實（不算分，但影響判斷）：**

- 開發機**尚未安裝 Rust 與 Go**；Python 3.14 + onnxruntime 1.28 已就緒。
- 目標裝置的按鈕鍵碼**尚未量測**（P0 T3）。若落在廠商自訂 HID collection，
  Rust 的 `global-hotkey` 與 Python 的 `pynput` **都可能不適用**，Go 的 `robotgo` 亦然
  → 三棧可能都需要退回 Raw Input / HID API，此項目會重新洗牌。

---

## 決策門檻（事前承諾，避免事後找理由）

| 條件 | 行動 |
|---|---|
| Rust 總分 ≥ 4.0 且無致命缺陷 | **直接選 Rust + Tauri 2** |
| Rust 出現致命缺陷（某平台熱鍵／注入無解） | 跑完 Go，與 Python 二選一 |
| 三棧皆不達標 | **縮小平台範圍**（例：先只做 Windows），而不是硬撐三平台 |

「致命缺陷」定義：**會導致某平台無法達成驗收標準第 1 條**（按住 → 說 → 放開 → 出字）的問題。

---

## 後果

- 選定後，此 ADR 狀態改為 `Accepted`，並在 `docs/spike/` 保留三份完整測試報告作為證據。
- 未選中的棧不移除其 Spike 報告 —— 它們是未來重新評估的基準線。
- 若日後要換棧，必須開新 ADR（`0002-…`），不得直接改寫本檔的結論。

---

## 待補

- [ ] 填入 P1（Python）Spike 分數與報告連結
- [ ] 填入 P2（Rust + Tauri）Spike 分數與報告連結
- [ ] 填入 P3（Go）Spike 分數與報告連結
- [ ] 補上 P0 T3 鍵碼結果對「全域熱鍵」可行性的影響
- [ ] 寫下最終決策與理由
